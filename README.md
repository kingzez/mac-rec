# Mac-Rec (Mac Recovery)
To recovery * thing

### Install Brew
```shell
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install"
```

### Install all of in Brewfile
```shell
brew bundle
```

### Install xcode cli tool
```shell
xcode-select --install
```

